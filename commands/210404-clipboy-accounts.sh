#!/bin/bash
## accounts
## version 0.0.1 - initial
##################################################
command-name-third-person-present() {
  echo "${command_name}s"
}
clipboy-accounts-default() {
  clipboy-say "Me $( command-name-third-person-present )"
}
clipboy-accounts-help() { 
  clipboy-accounts() {
    commands
  }
  unset -f clipboy-${command_name}-help
  unset -f clipboy-${command_name}-default
  clipboy-${command_name}
}
clipboy-accounts() { { local command_type ; command_type="$( echo ${1} | cut '-d:' '-f2' )" ; }
  local command_name
  local coinbase_api_key
  local coinbase_api_secret
  command_name="accounts"
  coinbase_api_key=$( clipboy store:get coinbase-api-key )
  coinbase_api_secret=$( clipboy store:get coinbase-api-secret )
  case "$( declare -f ${FUNCNAME}-${command_type} )" in
   "") {
     ${FUNCNAME}-default
   } ;;
   *) {
     ${FUNCNAME}-${command_type} ${@:2}
   } ;;
  esac
}
##################################################
## generated by create-stub2.sh v0.1.2
## on 
## see <https://github.com/temptemp3/sh2>
##################################################
